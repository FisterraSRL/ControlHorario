{
  "name": "controlhorario",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Control de fichadas: detección de irregularidades sobre exportaciones QUICKPASS.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
