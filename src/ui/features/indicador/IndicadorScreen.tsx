import { PlaceholderScreen } from '../../app/PlaceholderScreen.js';

/** PLACEHOLDER. See `src/ui/app/PlaceholderScreen.tsx`. */
export function IndicadorScreen() {
  return (
    <PlaceholderScreen
      titulo="Indicador de notificaciones"
      descripcion="Cantidad de faltas detectadas por persona en el período seleccionado."
      slice="slice 2b"
    />
  );
}
