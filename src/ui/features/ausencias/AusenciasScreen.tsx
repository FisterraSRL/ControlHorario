import { PlaceholderScreen } from '../../app/PlaceholderScreen.js';

/** PLACEHOLDER. See `src/ui/app/PlaceholderScreen.tsx`. */
export function AusenciasScreen() {
  return (
    <PlaceholderScreen
      titulo="Registro de ausencias"
      descripcion="Días sin fichadas, con su motivo y su documentación de respaldo, consultables por persona y por sector."
      slice="slice 2b"
    />
  );
}
