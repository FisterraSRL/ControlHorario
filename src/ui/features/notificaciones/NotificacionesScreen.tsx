import { PlaceholderScreen } from '../../app/PlaceholderScreen.js';

/** PLACEHOLDER. See `src/ui/app/PlaceholderScreen.tsx`. */
export function NotificacionesScreen() {
  return (
    <PlaceholderScreen
      titulo="Notificaciones a generar"
      descripcion="Fichadas incompletas, exceso de descanso y llegadas tarde del período seleccionado, agrupadas por sector y persona, con la generación de los Word."
      slice="slice 2b"
    />
  );
}
